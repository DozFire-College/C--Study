<Window x:Class="ProgressBarApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProgressBar Demo" Height="432" Width="954"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Прогресс загрузки" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <ProgressBar Grid.Row="1" x:Name="progressBar" 
                     Height="30" Minimum="0" Maximum="100" 
                     Value="0" Margin="0,0,0,10"/>


        <Grid Grid.Row="3" HorizontalAlignment="Center" 
              VerticalAlignment="Center">
            <StackPanel x:Name="congratulationsPanel" 
                        Visibility="Collapsed">
                <Image Source="congratulations.png" 
                       Width="200" Height="200"
                       Stretch="Uniform" Margin="0,0,0,10"/>
                <TextBlock Text="Поздравляем! Загрузка завершена!" 
                           FontSize="20" Foreground="Green" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Button Content="Загрузить быстрее" HorizontalAlignment="Left" Margin="371,73,0,0" Grid.Row="3" VerticalAlignment="Top" Height="46" Width="150" Click="Button_Click"/>
    </Grid>
</Window>